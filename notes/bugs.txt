
mobile: scroll jitter on bottom of feed - 2posts
mobile: can access verify-link url while not logged in

backend: follows resolver assumes raw/entities same length and can throw - medium
backend: activity log accepts invalid like/follow payloads without targets - medium
backend: typeorm synchronize=true in app module - medium
backend: deletePost flow is not transactional (activity delete + post remove split) - medium
backend: activity resolver throws generic Error instead of framework exception - low
backend: user gets throttled on verify but can change mail

client: on email change both isemailused and changeemail are called. 
if throttled the user can time oracle