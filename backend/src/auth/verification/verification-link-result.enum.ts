export enum VerificationLinkResult {
    VERIFIED = "verified",
    ALREADY_VERIFIED = "already_verified",
    EXPIRED_RESENT = "expired_resent",
    EXPIRED_THROTTLED = "expired_throttled",
    EXPIRED_DELIVERY_FAILED = "expired_delivery_failed",
    INVALID = "invalid"
}
